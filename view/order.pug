extends top-layout.pug
//- extends를 사용하려면 block으로 나눠서 넣어야함
block content
    header 주문 페이지 
    a(href="/") 메인으로 이동
    h1 주문 도서 목록

    div
        p 도서 목록
    form
        label 주문자 : 
        input(type="text", name="name") 
        br
        label 배송지 우편번호 :
        input(type="text", name="zip-code")

        label 배송지 기본주소 : 
        input(type="text", name="addr")

        label 배송지 상세주소 :
        input(type="text", name="detail-addr")
        br

        label 신용카드 번호 :
        input(type="text", name="card-num")

        label 신용카드 유효기간 :
        input(type="text", name="card-valid-date")
        label 신용카드 종류 :
        input(type="text", name="card-type")

        br

        p 주문 총액

        p 주문 할인 금액

        p 주문 최종 금액

        input(type="submit",value="제출")
